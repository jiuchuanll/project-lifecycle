# Cursor installation

Candidate version: `0.1.0`. Evidence status: `NOT_TESTED` until the native matrix is complete.

Add the repository through Cursor's native local Plugin directory or marketplace UI using `.cursor-plugin/plugin.json`. Use a disposable profile and pin this exact candidate version.

Reload Cursor, verify `maintain-project-knowledge` and `run-prd-lifecycle`, and run `bin/project-lifecycle version` in the project terminal. Remove the native Plugin entry and reload to uninstall. Record the Cursor version, manifest path, and redacted diagnostic if either Skill is absent.
