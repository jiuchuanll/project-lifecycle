# 贡献指南

[English](CONTRIBUTING.md)

感谢你为 Project Lifecycle 做出贡献。

## 分支流程

- 常规贡献以 `develop` 为目标分支。
- 稳定版本通过从 `release/*` 到 `main` 的拉取请求合入。
- 紧急修复使用 `hotfix/*`，并且只能通过拉取请求进入受保护分支。
- `main`、`develop`、`release/*` 和 `hotfix/*` 均要求 CI 通过并获得仓库所有者审核。
- 新提交会使之前的批准失效，并且合并前必须解决所有审核对话。

请在拉取请求中清楚说明变更，提供相关测试，并在行为变化时同步更新文档。请勿提交凭据、私有数据、本地生成状态或与特定机器绑定的路径。
